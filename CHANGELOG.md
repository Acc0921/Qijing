# 更新记录

## 2026-09-02（配置调度模块兼容边界）

- 确认配置替换模块本身不负责模式编译、前台触发和线程常驻调度，不能直接作为已支持调度器接入。
- 新增“配置调度器”固定身份探测；只有桥接契约、当前模式读回和固定切换入口同时可信时才生成四档模式计划，原模块缺少契约时保持不可选择。
- 模式执行继续经过参数白名单、原值快照、写后读回和恢复计划；不会调用模块的全局 cpuset 重置、卸载或应用数据清理脚本。
- 新增线程调度规则的有界解析与决策模型，支持包名、线程名精确/前缀匹配、CPU 集合、默认归属及 RR/nice 意图；当前不执行任何特权线程操作。
- 记录 clean-room 原生配置编译与线程调度引擎的能力边界、实施顺序和真机完成标准。
- JVM 119 项测试通过；Lint 无阻断错误，Debug/Release APK 构建通过。未在不兼容的 MIX 2S 上执行该模块或任何调度写入。

## 2026-09-02（真机只读验收与监控状态修复）

- 在 Xiaomi MIX 2S（Android 10）完成五个一级页面、应用扫描、CPU/内存/功耗只读数据和栖境自身窗口监控的真机验收；保持预览后端，未启动自动化或执行特权写入。
- 监控停止并生成摘要后，状态由误导性的“未记录”改为“本次会话已完成”，同时补充 Compose UI 回归断言。
- JVM 测试与 Debug APK 构建通过；Android 15 模拟器 MainActivityTest 8 项通过。

## 2026-09-02（五页观感与数据界面重构）

- 以移动端调度工具参考稿为输入，提炼灰绿背景、低阴影成组表面、功能色标记、稳定底栏和数据矩阵语言；未照搬其中的网页容器、虚假 Root 状态或越界调节能力。
- 总览改为就绪核心与连续状态组；应用改为可扫读的圆角连续列表；场景突出执行保障和对象关系；调节采用四项分段导航与设备数据组；监控形成独立会话面板和共享指标带。
- 五页 Top App Bar 统一标题、简短任务副标题和页面功能色；系统字体放大时收敛非必要副标题并重排状态核心，避免固定栏位裁切关键信息。
- 视觉调整不改变执行后端、能力白名单、预演、快照、写后读回、恢复、日志与数据模型。
- JVM 112 项测试通过；Android 6/15 模拟器均完成 15 项设备测试且 0 失败，Root-only 用例按条件跳过；Lint 与 Debug/Release APK 构建通过，并完成亮色、深色和 200% 字体人工走查。

## 2026-09-02（真机装机问题修复）

- 未完成场景新增独立新版本持久化；应用对象、调节意图和优先级可在强停后继续，能力探测与预演结果仍必须重新生成。
- Usage Stats 首次读取增加明确检查态，避免尚未确认时把权限显示为缺失；返回应用后继续刷新真实授权状态。
- 总览右上角改为可发现的任务记录入口，内部能力标识转换为用户可读的预演、验证、恢复与异常语言。
- FPS 改称“渲染 FPS”，不再用静态页面低渲染频率宣称流畅或卡顿，并引导结合帧耗时、P95 与卡顿报告判断。
- JVM 112 项测试通过；Android 6/15 模拟器均完成 15 项设备测试且 0 失败，Root-only 用例按条件跳过；Lint 无阻断错误，Debug/Release APK 构建通过。

## 2026-09-01（发布阻断问题整改第一批）

- 主导航改为 Navigation Compose 返回栈；场景编辑状态迁入 `ViewModel + SavedStateHandle`，底部栏目切换和进程重建不再静默丢失草稿，系统返回键优先返回场景列表。
- FPS 改按单调时钟的真实采样窗口计算，不再以渲染耗时总和充当经过时间；新会话保存逐帧分布并计算真实 P95，旧会话明确标为“P95 窗口均值”。
- 遥测从每秒重写完整 SharedPreferences 历史改为追加式会话文件，并以摊销压缩保留最近样本，降低长时监控对被测窗口的干扰。
- 自动化前台服务由受 Android 15 时限约束的 `dataSync` 改为带用途说明的 `specialUse`；加入安全重启、系统超时、服务心跳和陈旧状态校准，移除 `onDestroy` 主线程阻塞恢复。
- 应用页默认筛选有桌面入口的触发对象，内部软件包移入专家范围并标记“触发资格未知”；应用扫描和图标解码移出主线程。
- CPU/GPU 频率节点返回 0 时改为“未活动/设备上报无效”，不再展示误导性的 `0 MHz`。
- JVM 108 项测试通过；Android 6/15 模拟器均完成 13 项设备测试且 0 失败，Root-only 用例按条件跳过；Lint 0 error，Debug/Release APK 构建通过。

## 2026-09-01（全局调节与设备观察补全）

- 新增省电、均衡、性能、极速四档全局模式；场景可跟随全局配置或覆盖为指定意图，全局配置变化会停用旧预演场景。
- 新增系统、Uperf、UperfGT、fas-rs 调度控制方；第三方调度仅接受固定模块身份、固定路径、固定模式参数和写后读回，不开放任意 Shell。
- CPU 改为逐 `policyN` 快照、Governor/频率范围调节与恢复，并展示每核频率、在线状态、负载和策略域归属。
- 新增 GPU 当前/范围频率、负载、Governor 只读观察，以及 RAM、Swap、多 ZRAM 设备、压缩数据和算法状态。
- 新增电池电流、电压、温度及电池侧瞬时/估算功率；明确不将其包装为 CPU/GPU 分项功耗。
- 全局真实调节成功后保存可撤销原值计划；ZRAM 重建、GPU 写入、核心上下线、温控移除和任意 Shell 继续关闭。
- API 23/API 35 模拟器均完成 12 项设备测试且 0 失败；JVM 102 项、Lint、Debug/Release 构建通过。真实特权写入仍等待逐项授权验收。

## 2026-09-01（场景恢复 journal 与实时任务轨迹）

- 场景真实写入前同步持久化完整恢复计划，并以 `WRITE_STARTED/APPLIED/RESTORED` 逐项记录事务进度。
- 自动化进程重启后使用原执行后端逆序恢复；journal 损坏、进度保存失败或恢复不完整时锁定真实执行并保留处理证据。
- journal 进度使用事务 id 与 revision 比较更新；未知/错误后端、结果后端不一致和清理失败均保留恢复证据并拒绝继续写入。
- 总览与场景页新增结构化实时任务轨迹，关联命中、预检、快照、执行、读回验证、恢复和异常状态。
- 重启恢复移出服务主线程；dry-run 终态明确为“预演完成、无需恢复”，不再显示为活动写入场景。
- 真实场景执行缺少持久化 journal 时在首条写入前阻断；补充 JVM 与 API 23/API 35 设备契约测试。
- 未授权前台应用访问时不再保留假运行服务；尚未开始写入的 journal 可无特权连接安全清理。

## 2026-09-01（五页 Android 原生化重构）

- 五个一级入口统一改为紧凑 Top App Bar、原生底部导航、分组状态行和页面级滚动，移除英文眉题、欢迎语、巨型 Hero 与卡片流。
- 总览首屏收敛为就绪结论、唯一下一步、自动化/权限/执行方式状态；Root、Shizuku 与预览移入带能力和风险说明的选择 Sheet。
- 应用改为可扫读的系统列表，任务筛选移入 Bottom Sheet；选择应用继续携带完整对象进入场景链路。
- 场景保留“应用—意图—优先级—预演—启用—恢复”专属链路，高风险启用改为显示后端、原值、目标与恢复条件的确认 Sheet。
- 调节改为设备状态行、Governor 候选选择和 Swappiness 意图轨道；真实模式不再提供未知 Governor 自由输入，预演与执行事务保持不变。
- 监控改为一次明确的自身窗口观察会话，增加轻量帧时间趋势；开始/停止只保留一个当前主动作，历史与 CSV 导出降为次级列表操作。
- 接入 edge-to-edge、透明系统栏、导航状态保存及更克制的字体、间距与圆角；不把未完成的真机写入包装为可用能力。

## 2026-09-01（场景链路工作台第一阶段）

- 应用以完整对象进入场景链路，新增真实应用图标、已配置筛选和上下文连续转场。
- 场景改为“应用—意图—优先级—执行保障—恢复”链路；保存默认不启用，真实启用必须先完成同源安全预演。
- 新增零写入 `prepare` 阶段，预演与执行共用白名单、快照和恢复校验；命中时仍会重新准备，避免使用过期原值。
- 优先级、启用状态和未开放能力可保真持久化；同优先级冲突、ZRAM 与核心上下线会阻止真实启用。
- 全局执行方式变化会停用场景并要求重新预演，防止预览场景静默升级为 Root/Shizuku 写入。
- 预演结果绑定当次草稿和执行方式，编辑产生的异步旧结果不能再授权新草稿；普通保存也不能绕过预演直接启用。
- 自动化服务新增持久运行/停止/恢复未确认状态，恢复完成前锁定后端；停止超时或恢复失败会留下高危记录。
- 空调节目标、真实后端缺快照能力及最高优先级并列改为核心层零写入阻断。
- 同一前台应用不变时也会对场景启停做轻量协调，停用活动场景不再等待切换应用才恢复。
- 应用筛选、频率输入、主要动作和高风险确认适配小屏/大字体；意图轨道、场景开关与异步反馈补充无障碍语义。
- Android 15 模拟器 UI 测试通过；无 Root 模拟器明确跳过只适用于授权 Root 设备的握手用例。

## 2026-08-31（五栏产品架构与安全交互规范）

- 明确“总览、应用、场景、调节、监控”五个顶层入口，将 CPU 与内存/ZRAM 收敛到同一调节任务，避免按底层模块堆叠菜单。
- 固化首次就绪、应用到场景、安全调节、监控回看和异常处理五条用户任务路径，并要求跨栏跳转保留上下文。
- 根据知乎等成熟产品的 UI 学习结论，吸收稳定导航、清晰信息层级和及时反馈；同时补充 clean-room 边界，不复制旧版或第三方页面、资源和品牌视觉。
- 建立真实特权写入的预览、二次确认、防重复提交、读回验证与恢复结果展示规范。
- 增加 TalkBack、48 dp 触控目标、非颜色状态、200% 字体、亮暗主题对比度和小屏适配验收项。

## 2026-08-31（Android 17 真机测试兼容）

- 升级 AndroidX Test Runner、JUnit 与 Espresso，修复 Android 17 上旧版测试框架调用已移除隐藏 API 的问题。
- 固定 AndroidX Tracing 1.2.0，补齐 Espresso 3.7 所需 API，同时保持与当前 AGP/R8 构建链兼容。
- 增加真机 Root 只读握手测试，以 `id -u` 验证按应用授权、UID 0 返回与超时边界，不触发性能参数写入。
- 建立主力机安全验收边界：Root、系统性能写入、重启与权限策略变更必须在逐项说明并获得确认后执行。

## 2026-08-31（模拟器验收）

- 增加 AndroidX instrumentation runner 配置，修复 Compose UI 测试在设备端发现用例前崩溃的问题。
- 建立 Android 15 与 Android 6 模拟验收环境，用于覆盖安装、导航、权限和兼容性路径。
- 模块卡片点击后自动滚动到对应内容页，修复用户点击 M3/M8 后看不到页面变化的问题。
- UI 测试覆盖小屏 LazyColumn 导航，并验证 Android 6 上 M8 的明确降级提示。
- 新增仅存在于 debug 构建的调节模拟后端，覆盖四项白名单能力、快照、写后读回、故障注入、逆序回滚和持久化重启恢复。
- 根据模拟器截图修正模块卡片名称与编号挤在一起的问题，统一为左右对齐。

## 2026-08-31（栖境命名与真实后端主线）

- 产品名称统一为“栖境（Qijing）”，包名、namespace、源码/测试目录和全新数据文件统一迁移到 `com.qijing`。
- 接入 Root 固定白名单后端，支持 CPU governor、最低/最高频率与 swappiness，包含参数约束、超时、写后读回和稳定错误码。
- 接入 Shizuku SDK、显式授权、UserService transport 和同一固定能力白名单；Android 6 保持只读降级，Shizuku 从 Android 7 开始可用。
- 场景生命周期增加同场景去重、离场恢复、切换前恢复、前台事件源失效恢复和停止服务恢复。
- 真实后端执行前完成整批预检与快照完整性检查；任一能力不支持或原值不可读时不进行部分写入。
- ZRAM 重建继续保持关闭，等待厂商适配与真机恢复验证后再开放。

## 2026-08-31（M8 采集与用户体验校验）

- 新增 Android 7+ 当前窗口 `FrameMetrics` FPS 采集器，按 1 秒窗口聚合，避免逐帧写入拖慢界面。
- 新增显示刷新率感知的帧耗时、FPS 上限、卡顿阈值和丢失报告统计，并接入 M8 开始/结束汇总界面。
- 新增可注入的进程 ADB transport，保持命令策略只读；补充 FPS 数学与 transport 单元测试。
- 增加应用目录可见性声明和正式矢量图标，构建 lint 继续通过。

## 2026-08-31（M3/C4 可操作反馈）

- M3 增加已保存场景列表、启停开关和草稿回填编辑，减少重复输入。
- M1 增加最近任务结果卡片，让用户能看到场景执行或回滚反馈。
- 未授予 Usage Stats 时不创建场景轮询线程，首页同步禁用无效的启动操作。

## 2026-08-31（C3/C4 数据可靠性）

- 新数据层增加 schema 版本边界、并发写入保护和损坏数据安全降级。
- FPS 遥测限制每个会话最多保留 2,000 个聚合样本，任务日志继续限制 500 条。

## 2026-08-31（Compose 用户路径测试）

- 为首页、场景服务、M3 保存和 M8 采集控件增加稳定测试语义。
- 新增首次打开、M3 场景编辑和 M8 监控入口的 Compose UI 自动化用例。

## 2026-08-31（M2 到 M3 用户路径）

- 应用列表增加版本、系统/用户应用类型与场景操作提示。
- 点击应用可携带包名直接进入 M3 场景编辑，免去手动复制包名。

## 2026-08-31（M8 历史与导出）

- FPS 监控增加最近 Session 列表、摘要回看和最多 50 个会话的持久化索引。
- 增加无存储权限的 CSV 系统分享导出，并用 JVM 测试固定列定义、数值格式与转义规则。

## 2026-08-31

- 增强场景执行为 CommandPlan 事务，失败时按已执行顺序逆向回滚并记录结果。
- 增加 SharedPreferences 新数据层实现，持久化设备、应用、场景和 FPS 遥测。
- 增加 M1 设备总览、M2 应用筛选、M3 场景草稿与校验状态层。
- 接入本机 Android SDK/Gradle 构建配置，指定已安装 Build Tools 版本。
- 修正 AndroidX 构建开关，准备生成首个 debug APK。
- 增加可访问的 Maven 镜像源，改善受限网络下的构建稳定性。
- 调整 Gradle/Kotlin 编译内存，避免本机低堆配置触发 GC thrashing。
- 首次完成 `assembleDebug` 构建，生成可安装 debug APK。
- 增加 M4/M5 调节状态控制器、设备能力矩阵和 M8 FPS 会话摘要分析。
- 将首页入口升级为可交互模块导航，接入 M1 总览、M2 应用筛选和 M3 场景保存页面。
- 重新构建 debug APK，验证 UI 代码可编译。
- 将 UI 数据源切换为持久化新数据层，M3 增加逗号分隔的应用包名绑定。
- C2 增加只读 CPU/内存/ZRAM/GPU 路径和执行后端可用性探测，并接入 M1 展示。
- 增加 M3 前台应用场景选择器，以及 M4/M5 CPU、内存/ZRAM 只读状态读取器。
- 完成只读状态层构建验证，未启用任何系统写入操作。
- 增加 C0 只读 ADB 执行器与命令白名单，拒绝未授权写入能力。
- 增加场景选择器和命令策略单元测试。
- M4/M5 页面接入真实只读 CPU、内存和 ZRAM 状态，并支持手动刷新。
- 增加 FPS 会话摘要和场景草稿校验测试。
- M4/M5 页面接入只读状态卡片和手动刷新操作。
- 测试扩展到 7 个核心用例，覆盖调节校验、场景选择和 FPS 摘要。
- 增加第一版功能实施核对清单，区分可用、骨架、未实现和明确排除项。
- C4 增加有界持久化任务日志，场景执行记录可跨重启读取。
- 修正任务日志边界测试断言，8 个核心单元测试全部通过。
- C1 增加执行前能力快照、restore command 绑定和前台事件协调器接口。
- 增加 Sysfs 恢复值读取器和场景快照测试。
- C1 场景执行现在支持快照值绑定 restore command，供后续真实后端执行回滚。
- 增加前台应用事件协调器接口，保持事件源与场景决策解耦。
- 增加 Usage Stats 前台应用只读事件源和特殊访问权限状态检查。
- 对 Usage Stats 特殊权限增加局部 lint 说明，保持构建检查可通过。
- 增加可控的前台应用轮询循环，事件去重且不直接触发系统写入。
- 增加轮询去重单元测试。
- 修正前台事件源接口为可用函数式接口，轮询测试和构建全部通过。
- 增加可选前台场景服务，承载轮询并在停止时释放资源，默认不自动启动。
- 为前台服务补齐 dataSync 权限声明，并消除旧版 Notification.Builder 编译告警。
- 首页增加场景服务启动/停止按钮和 Usage Stats 权限设置引导，返回页面自动刷新授权状态。
- 完成服务控制 UI 的构建、lint 和单测验证。
- 增加用户视角验收矩阵、必测路径和发布门槛。
- 增加 GitHub 同类工具公开基准，并转化为 M2/M3/M4/M5/M8 的设计要求。
- 增加首次使用就绪度评估模型和测试。
- 建立第一版重构工程与模块边界。
- 固化 C0-C5、M1/M2/M3/M4/M5/M8 范围。
- 增加 APK 静态解析功能映射文档。
- 增加特权执行、场景引擎、全新数据层、CPU/内存调节和 FPS 监控接口骨架。
- 增加 Compose 首页 UI 骨架，默认使用 dry-run 执行策略。
- 公开工程改为 clean-room 设计表述，移除旧产品标识和旧数据结构名称。
- 清理公开源码中的历史结构关键词，保持产品文案与实现独立。
